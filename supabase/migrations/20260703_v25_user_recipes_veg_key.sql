-- ================================================================
-- user_recipes に veg_key を追加
-- 2026-07-03
--
-- recipesテーブルにはアイコン・科の判定用にveg_key（'tomato'等、
-- PRESET_VEGSのidと一致）を持たせていたが、購入時にuser_recipesへ
-- コピーし忘れており、「野菜を追加」ダイアログでアイコンが
-- 自動選択されない不具合の原因になっていた。
-- ================================================================

ALTER TABLE user_recipes ADD COLUMN IF NOT EXISTS veg_key TEXT;
